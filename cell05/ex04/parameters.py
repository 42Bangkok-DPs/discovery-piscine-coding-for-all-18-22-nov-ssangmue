#!/usr/bin/env python3
import sys

# Get the number of parameters passed (excluding the script name)
num_parameters = len(sys.argv) - 1

# Print the result
print(f"Number of parameters: {num_parameters}.")
